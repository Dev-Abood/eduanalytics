/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you ever host images from external domains, add them here:
  // images: {
  //   domains: ["example.com"],
  // },
};

module.exports = nextConfig;
